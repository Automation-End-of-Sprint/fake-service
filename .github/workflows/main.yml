name: Property Validation PROD

on: 
  workflow_dispatch:
env:
  SCRIPT_DIR: scripts
  MASTER_DIR: master
  DEVELOP_DIR: develop

jobs:
  checkout-script:
    name: Hello world
    runs-on: ubuntu-latest
    steps:
    - name: Check-out prod validation script repository
      uses: actions/checkout@v2
      with:
        repository: Automation-End-of-Sprint/Prod-Property-Validation-Script
        token: ${{ secrets.GITHUB_TOKEN }}
        path: ${{ env.SCRIPT_DIR }}
        ref: main
    - name: Check-out mgmt-master repository
      uses: actions/checkout@v2
      with:
        repository: Automation-End-of-Sprint/mgmt-config-fake
        token: ${{ secrets.GITHUB_TOKEN }}
        path: ${{ env.MASTER_DIR }}
        ref: master
        
    - name: Check-out mgmt-develop repository
      uses: actions/checkout@v2
      with:
        repository: Automation-End-of-Sprint/mgmt-config-fake
        token: ${{ secrets.GITHUB_TOKEN }}
        path: ${{ env.DEVELOP_DIR }}
        ref: develop

    - name: Hello World
      run: tree
      shell: sh
